CREATE TABLE user(
  id INT NOT NULL AUTO_INCREMENT,
  name VARCHAR(20) NOT NULL,
  age INT NOT NULL,
  PRIMARY KEY (id)
);

INSERT INTO user(id, name, age) VALUES
(1,'zsy',32),
(2,'cjj',32),
(3,'crg',33);

